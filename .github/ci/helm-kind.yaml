# kind e2e — local images + builtin PG. No CNPG/Gateway/Apprise/NP.

image:
  repository: xrelease
  tag: "dev"
  pullPolicy: Never

ui:
  image:
    repository: xrelease-ui
    tag: "dev"
    pullPolicy: Never
  replicaCount: 1

ingress:
  enabled: false
gateway:
  enabled: false

postgresql:
  mode: builtin
  enabled: true

apprise:
  enabled: false

networkPolicy:
  enabled: false

metrics:
  serviceMonitor:
    enabled: false

secrets:
  adminPassword: ci
