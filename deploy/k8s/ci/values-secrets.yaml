# CI-only secrets for helm template/lint (fake values). Not for real clusters.

postgresql:
  auth:
    password: "ci-test-pg"

secrets:
  apiKey: "ci-test-key"
  webhookSecret: "ci-test-secret"
  sessionSecret: "ci-test-session-secret-0123456789abcdef"
  adminUser: "admin"
  adminPassword: "ci-test-admin"
  configEncryptionKey: "MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY="
