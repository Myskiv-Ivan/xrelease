apiVersion: v1
kind: Service
metadata:
  name: {{ include "xrelease.fullname" . }}
  labels:
    {{- include "xrelease.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  # Component-scoped: UI, Apprise and PostgreSQL pods carry the same
  # name+instance labels and would otherwise land in these endpoints.
  selector:
    {{- include "xrelease.componentSelectorLabels" (dict "ctx" . "component" "server") | nindent 4 }}
